node_modules/
.npm-cache/

# Expo
.expo/
dist/
web-build/
expo-env.d.ts
.metro-health-check*

# Native
.kotlin/
*.orig.*
*.jks
*.p8
*.p12
*.key
*.mobileprovision
/ios
/android

# debug
npm-debug.*
yarn-debug.*
yarn-error.*

# env
.env
.env*.local
.collection/

# typescript
*.tsbuildinfo

# macOS
.DS_Store
*.pem

# design-sync
.ds-sync/
ds-bundle/
.design-sync/.cache/
.design-sync/learnings/
.design-sync/node_modules
packages/ui/dist/

# 화면 확인용 임시 캡처 — 저장소에 남기지 않는다
.tmp-shots/

# 로컬 검수·격리 체크아웃 (공유 검수 기록은 docs/audits/에 보관)
/.audit-*/
/.audit-*.cjs
/.audit-*.log
/.tmp-chrome-audit/
/.worktrees/
/WeddingPickl/
# 로컬 자격증명 전달 폴더
/iOS_액세스 토큰(중요)/

# CI가 만드는 마케팅 미리보기 산출물(main.yml의 MARKETING_PREVIEW_DIR).
# 로컬에서 그 단계를 돌려 보면 생긴다 — 저장소에 넣지 않는다.
.marketing-preview/
